name: Test ignore-warnings flag
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Run action with warnings-only report and ignore-warnings enabled
        id: action
        uses: .
        with:
          report-path: tests/fixtures/warnings-only-report.xml
          ignore-warnings: 'true'
      - name: Assert action succeeded with warnings ignored
        run: |
          echo "PASS: action completed successfully with ignore-warnings=true"
